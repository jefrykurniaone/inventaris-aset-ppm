# Sub-agent git worktrees, each carrying its own node_modules and .next.
.claude

node_modules
.next
out
build
coverage
playwright-report
test-results
# Playwright MCP session snapshots. Ignored by `.gitignore` too, but Prettier
# does not read that file, so both entries are needed.
.playwright-mcp
.storage
src/generated
# The vendored browser-image-compression worker: a verbatim copy of the
# published dist build, asserted byte-identical by
# src/lib/photo-compression.test.ts. Reformatting it breaks that assertion.
public/vendor
package-lock.json
next-env.d.ts
*.ico
*.svg

# Prose documentation is hand-wrapped to a fixed column and its tables are
# written unpadded. Prettier reflows both, which buries real edits in noise.
*.md
