/**
 * Seed stub.
 *
 * `prisma.config.ts` points `migrations.seed` here, so `prisma migrate reset`
 * and `prisma db seed` need this file to exist and to exit cleanly. The real
 * seed data — categories, locations, and a first administrator — belongs to the
 * seeding ticket. Nothing is written to the database yet.
 */
console.info("prisma/seed.ts: no seed data yet; nothing was written.");
